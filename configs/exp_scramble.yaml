# Spatial scrambling control: does the causal patch effect depend on the
# donor's SPATIAL ARRANGEMENT, or just its channel/statistical content?
# Compare intact donor patch vs the same donor with tokens spatially permuted.
counts: [2, 5]
objects: [cat, dog]
seeds: [0, 1, 2, 3, 4, 5, 6, 7]
num_inference_steps: 30
score_threshold: 0.3
patch_block: up_blocks.0
patch_attn: attn1
patch_steps: [0, 1, 2, 3, 4, 5]
pairs: [[2, 5], [5, 2]]
